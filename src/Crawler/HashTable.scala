package Crawler

/**
 * single instance 
 * @author MD103
 */
object HashTable {
 private val singleInstance = List[HashCode]()
  
  def isIdentical(hash : HashCode) {}
  
  def isNearDuplicates(hash : HashCode) {}
  
  
}